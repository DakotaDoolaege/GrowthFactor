using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerSpotActivator : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

   public 

    // Update is called once per frame
    void Update()
    {
        
    }
}
